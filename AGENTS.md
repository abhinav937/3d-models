# T-Clip FreeCAD Project - Session Log (v6+)

## Scope

This file intentionally tracks only `v6` and newer revisions.
All notes for versions before `v6` were removed.

## Current FreeCAD State

- Active document: `t_clip`
- Active object: `T_Clip_v8`

## Verified From FreeCAD MCP (2026-04-09)

| Property | Value |
|----------|-------|
| Bounding box X | `-10.00` to `+10.00` mm |
| Bounding box Y | `-2.00` to `+10.52` mm |
| Bounding box Z | `0.00` to `+12.00` mm |
| Overall size | `20.00 x 12.52 x 12.00` mm |
| Volume | `1294.31987 mm^3` |
| Surface area | `1502.64890 mm^2` |
| Topology | `67 faces`, `222 edges`, `159 vertices` |

## Geometry Markers (T_Clip_v8)

- Horizontal span reaches `X=+-10.0`.
- Inner horizontal slot floor at `Y=0.0`.
- Horizontal slot roof near `Y=1.52`.
- Main T-junction shelf around `Y=3.52`.
- Vertical body top reaches `Y=10.52`.
- Major top plane at `Z=10.0` and full height to `Z=12.0`.

## Notes

- The active `.FCStd` session currently shows only one object: `T_Clip_v8`.
- No pre-`v6` model objects are present in the open `t_clip` document at this time.

